name: Pull Request

on:
  push:
    branches:
      - main

jobs:
  build:
    name: Build
    uses: ./.github/workflows/build.yml
  deploy:
    name: Deploy
    uses: ./.github/workflows/deploy.yml
    needs: build
    with:
      environment: staging
      uuid: ${{ needs.build.outputs.uuid }}