name: Deploy

on:
  workflow_call:
    inputs:
      dest: 
        required: true
        type: string
      sha:
        required: true
        type: string

jobs:
  deployment:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy
        run: |
          echo "Deploying to ${{ inputs.dest }} with ${{ inputs.sha }} "