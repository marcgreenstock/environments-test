name: Deployment

on: push

jobs:
  staging:
    runs-on: ubuntu-latest
    environment: 
      name: staging
    steps:
      - name: deploy
        run: |
          echo "hello world"
  production:
    runs-on: ubuntu-latest
    environment: 
      name: production
    steps:
      - name: deploy
        run: |
          echo "hello world"

